# Vault index

Modules: [[libs/core/00-overview|core]] and [[app/00-overview|app]].

The style guide shows the link form as `[[libs/parser-core/00-overview|parser-core]]`, which must not be treated as a link.

![[diagram.png]]

| Module | Role |
| --- | --- |
| core | primitives |

$$
b = \min(C, b + (t - t_{\mathrm{last}}) r)
$$
